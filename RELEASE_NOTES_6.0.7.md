# AppDock 6.0.7 — Optional Sleep Screen

AppDock 6.0.7 adds one small optional feature: enable **Settings → Other → Sleep screen** to show a simple AppDock screen when KOReader enters sleep instead of the standard KOReader sleep message.

The screen contains only the AppDock name and “Sleeping” on a plain E-Ink-friendly background. It closes automatically when the device resumes. The feature is disabled by default, stored locally, does not change Simple Mode, and does not add any new sleep behavior.

Lua 5.1 syntax, the focused suspend/resume regression, existing Core and Browser suites, root/package mirror checks and archive validation passed before release.
